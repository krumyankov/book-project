pipeline{
        agent any
        stages{
                stage('Run command'){
                steps{
                    sh "docker-compose up"
                }
            }
            
        }
}
