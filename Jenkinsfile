pipeline{
        agent any
        stages{
                stage('Install compose'){
                        sh 
                
                stage('Run command'){
                steps{
                    sh "docker-compose up"
                }
            }
            
        }
}
